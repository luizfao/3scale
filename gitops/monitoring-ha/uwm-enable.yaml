# Habilita o User Workload Monitoring (UWM) no cluster HA.
# Mesmo ConfigMap do cluster primário — apenas ativa o UWM.
#
# Verificar após o sync:
#   oc -n openshift-user-workload-monitoring get pods
# Esperado: prometheus-user-workload-0, thanos-ruler-user-workload-0 em Running.
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-monitoring-config
  namespace: openshift-monitoring
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
data:
  config.yaml: |
    enableUserWorkload: true
---
# Configura o Prometheus UWM do cluster HA com external_labels.
# O label site=ha identifica a origem das séries no Grafana do cluster primário.
# Evite "cluster" (reservado pelo CMO) e "instance" (label por target no Prometheus).
#
# NOTA: O namespace openshift-user-workload-monitoring só existe após o UWM ser habilitado
# (ConfigMap acima). Na primeira execução deste Application o sync pode falhar para este
# recurso. Execute o sync novamente após o UWM estar ativo (pods Running).
apiVersion: v1
kind: ConfigMap
metadata:
  name: user-workload-monitoring-config
  namespace: openshift-user-workload-monitoring
  annotations:
    argocd.argoproj.io/sync-wave: "10"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
data:
  config.yaml: |
    prometheus:
      externalLabels:
        site: ha
