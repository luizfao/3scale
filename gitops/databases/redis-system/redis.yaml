# Redis for 3scale System component (Sidekiq, etc.) - required in 2.16
# GitOps version based on 3scale migration guide for external Redis deployments.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: system-redis-external
  namespace: 3scale-databases
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  labels:
    app: system-redis-external
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: system-redis-storage-external
  namespace: 3scale-databases
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  labels:
    app: system-redis-external
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ocs-external-storagecluster-cephfs
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
---
apiVersion: v1
kind: Service
metadata:
  # Kept as system-redis to preserve existing secret URL in 3scale-gitops
  name: system-redis
  namespace: 3scale-databases
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  labels:
    app: system-redis-external
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    deployment: system-redis-external
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: system-redis-external
  namespace: 3scale-databases
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app: system-redis-external
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      deployment: system-redis-external
  template:
    metadata:
      labels:
        deployment: system-redis-external
    spec:
      serviceAccountName: system-redis-external
      terminationGracePeriodSeconds: 30
      containers:
        - name: system-redis-external
          image: registry.redhat.io/rhel9/redis-7
          imagePullPolicy: IfNotPresent
          env:
            - name: REDIS_CONF
              value: /etc/redis.d/redis.conf
          ports:
            - containerPort: 6379
              protocol: TCP
          readinessProbe:
            exec:
              command:
                - container-entrypoint
                - bash
                - -c
                - redis-cli set liveness-probe "`date`" | grep OK
            initialDelaySeconds: 10
            timeoutSeconds: 1
            periodSeconds: 30
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 6379
            initialDelaySeconds: 10
            timeoutSeconds: 1
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 3
          resources:
            limits:
              cpu: "2"
              memory: 2Gi
            requests:
              cpu: "250m"
              memory: 256Mi
          volumeMounts:
            - name: system-redis-storage-external
              mountPath: /var/lib/redis/data
            - name: redis-config-external
              mountPath: /etc/redis.d/
      volumes:
        - name: system-redis-storage-external
          persistentVolumeClaim:
            claimName: system-redis-storage-external
        - name: redis-config-external
          configMap:
            name: redis-config-external
            items:
              - key: redis.conf
                path: redis.conf
